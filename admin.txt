admin
	name 
	email
	password
	role


customer
	c_id
	name
	mob_no
	address
	delivery 
	gst_no

Stock
	p_id
	pro_name
	qty_type
	qantity
	hsa/sac

Bill
	b_id
	c_id
	date
	status

order
	b_id
	p_id
	amount
	qnty


create table bill (
	b_id int,
	c_id int,
	b_date date,
	b_status varchar(20),
	FOREIGN KEY (c_id) REFERENCES cutomer(c_id)
)

create table order(
	o_id int,
	b_id int,
	p_id int,
	amount float,
	FOREIGN KEY (p_id) REFERENCES product(p_id),
	FOREIGN KEY (b_id) REFERENCES bill(b_id)
)
